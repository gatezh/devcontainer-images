name: Build devcontainer-claude

on:
  push:
    branches:
      - master
    paths:
      - 'devcontainer-claude/.devcontainer/Dockerfile'
      - 'devcontainer-claude/.devcontainer/init-firewall.sh'
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: devcontainer-claude

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Extract versions from Dockerfile
        id: versions
        run: |
          DOCKERFILE="devcontainer-claude/.devcontainer/Dockerfile"

          NODE_VERSION=$(grep '^ARG NODE_VERSION=' "$DOCKERFILE" | cut -d'=' -f2)
          CLAUDE_CODE_VERSION=$(grep '^ARG CLAUDE_CODE_VERSION=' "$DOCKERFILE" | cut -d'=' -f2)
          GIT_DELTA_VERSION=$(grep '^ARG GIT_DELTA_VERSION=' "$DOCKERFILE" | cut -d'=' -f2)

          echo "node=$NODE_VERSION" >> $GITHUB_OUTPUT
          echo "claude_code=$CLAUDE_CODE_VERSION" >> $GITHUB_OUTPUT
          echo "git_delta=$GIT_DELTA_VERSION" >> $GITHUB_OUTPUT

          echo "Extracted versions:"
          echo "  Node: $NODE_VERSION"
          echo "  Claude Code: $CLAUDE_CODE_VERSION"
          echo "  Git Delta: $GIT_DELTA_VERSION"

      - name: Generate image tags
        id: tags
        run: |
          NAMESPACE=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          BASE_IMAGE="${{ env.REGISTRY }}/${NAMESPACE}/${{ env.IMAGE_NAME }}"

          NODE_VERSION="${{ steps.versions.outputs.node }}"

          VERSION_TAG="node${NODE_VERSION}"

          TAGS="${BASE_IMAGE}:latest,${BASE_IMAGE}:${VERSION_TAG}"

          echo "namespace=$NAMESPACE" >> $GITHUB_OUTPUT
          echo "tags=$TAGS" >> $GITHUB_OUTPUT
          echo "Generated tags:"
          echo "$TAGS" | tr ',' '\n'

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ steps.tags.outputs.namespace }}/${{ env.IMAGE_NAME }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: devcontainer-claude/.devcontainer
          file: devcontainer-claude/.devcontainer/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.tags.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Verify image
        run: |
          TAGS="${{ steps.tags.outputs.tags }}"
          FIRST_TAG=$(echo "$TAGS" | cut -d',' -f1)

          echo "Verifying multiplatform image: $FIRST_TAG"
          docker pull --platform linux/amd64 "$FIRST_TAG"
          docker pull --platform linux/arm64 "$FIRST_TAG"

          echo "Multiplatform image successfully built and pushed!"
          echo "Tags: $TAGS"
